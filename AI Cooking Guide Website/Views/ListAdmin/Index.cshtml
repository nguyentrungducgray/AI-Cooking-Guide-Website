@model AI_Cooking_Guide_Website.Models.AddModel

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<body>
    <!-- MAIN -->
    <main>
        <div class="head-title">
            <div class="left">
                <h1>Food List</h1>
                <ul class="breadcrumb">
                    <li>
                        <a href="@Url.Action("Index", "Dashboard")">Dashboard</a>
                    </li>
                    <li><i class='bx bx-chevron-right'></i></li>
                    <li>
                        <a class="active" href="#">Food List</a>
                    </li>
                </ul>
            </div>
            <a href="@Url.Action("Add", "Admin")" class="btn-add">
                <i class='bx bx-plus-circle'></i>
                <span class="text">Add New Food</span>
            </a>
        </div>

        <!-- TABLE -->
        <div class="table-container">
            <table class="styled-table">
                <thead>
                    <tr>
                        <th>Serial Number</th>
                        <th>Image</th>
                        <th>Name</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Recipes != null && Model.Recipes.Any())
                    {
                        foreach (var recipe in Model.Recipes)
                        {
                            <tr>
                                <td>@recipe.Id</td>
                                <td>
                                    <img src="@recipe.Image" alt="@recipe.Name" class="food-image" style="width: 100px; height: auto;" />
                                </td>
                                <td>@recipe.Name</td>
                                <td>
                                    <!-- Corrected Edit Button -->
                                    <a href="@Url.Action("Edit", "ListAdmin", new { id = recipe.Id })" class="btn-edit">Edit</a>
                                    <!-- Delete Button -->
                                    <form action="@Url.Action("Delete", "ListAdmin", new { id = recipe.Id })" method="post" style="display:inline;">
                                        @Html.AntiForgeryToken() <!-- CSRF Token for security -->
                                        <input type="hidden" name="_method" value="DELETE" /> <!-- Simulate DELETE method -->
                                        <button type="submit" class="btn btn-delete" onclick="return confirm('Are you sure you want to delete this recipe?');">
                                            <i class='bx bx-trash'></i>Delete
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="4">No recipes found.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <!-- TABLE -->
    </main>
</body>
